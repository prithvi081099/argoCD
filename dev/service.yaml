apiVersion: v1
kind: Service
metadata:
  name: githubaction-svc
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  selector:
    app: githubaction
  ports:
  - name: http
    protocol: "TCP"
    port: 8080
    targetPort: 8080